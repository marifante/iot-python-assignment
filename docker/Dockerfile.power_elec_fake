FROM python:3.7.4-slim

WORKDIR /app

COPY requirements-dev.txt dev/power_elec_fake.py /app/

RUN pip install --no-cache-dir -r /app/requirements-dev.txt

ENTRYPOINT ["python", "/app/power_elec_fake.py"]
